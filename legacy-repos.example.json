{
  "description": "Optional legacy checkouts for /legacy-spec — empty for greenfield products",
  "defaultProject": null,
  "projects": {
    "legacy-backend": {
      "root": "../legacy-backend",
      "description": "Example legacy backend — read-only for /legacy-spec",
      "write": false
    },
    "legacy-frontend": {
      "root": "../legacy-frontend",
      "description": "Example legacy frontend — read-only reference",
      "write": false
    }
  }
}
