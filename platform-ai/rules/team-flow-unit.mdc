---
description: Unit test lane — Vitest, hooks/services
globs: tests/unit/**/*
alwaysApply: false
---

# Unit tests

- Logic only — no full page mount unless testing a small hook with `renderHook`
- Mock `apiFetch` / service at boundary (`tests/unit/_helpers/mockApiFetch.ts`)
- Generated hook tests: `tests/unit/hooks/{entity}/use*List.test.ts`
