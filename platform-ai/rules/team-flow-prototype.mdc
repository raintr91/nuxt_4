---
description: /prototype, /grill-prototype — UI prototype with mock API boundary
globs: "{pages,components,composables,mocks}/**/*.{vue,ts}"
alwaysApply: false
---

# Team Flow — Prototype

Active commands: `/prototype`, `/grill-prototype` — one per session.

| Command | Skill |
|---------|-------|
| `/prototype` | `.cursor/skills/prototype/SKILL.md` |
| `/grill-prototype` | `.cursor/skills/grill-prototype/SKILL.md` |

Shared: resolve skill `extractBundle` → `.cursor/extracts/extract-registry.json` (e.g. `prototype`, `dev-grill`).

Prerequisite: spec đã qua `/dev-grill-docs` (hoặc `/grill-with-docs`) — `pnpm portal:gen:dry` passed. Hub: `docs/operational/PORTAL-CODEGEN.md`.

## Thứ tự

1. **Quét `#needs-component`** — `tags:`, columns `render: custom`; chỉ tạo `Mo*` / slot UI còn thiếu.
2. **`pnpm portal:gen --spec docs/features/yaml/.../ir/spec.yaml`** — scaffold; đọc `{function}/generated/HANDOFF.md`.
3. **Sau gen** — slot → component, auth bypass, gap; `docs:render` cập nhật Screen link trong `.md` (không sửa YAML spec).

- Source: grill-approved `docs/features/yaml/.../{function}/ir/spec.yaml`
- Real UI/actions; mock only API response boundary
- **Không** real backend; `/wire` replaces mocks
- Gắn `data-testid` sớm (`docs/operational/E2E-TESTIDS.md`)
- **Không** duplicate scaffold đã gen — agent review + HANDOFF + component nhỏ
