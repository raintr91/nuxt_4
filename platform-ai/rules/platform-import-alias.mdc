---
description: Import alias — @/ trong src
globs: src/**/*
alwaysApply: false
---

# Import alias

- Use `@/` → `src` (no `../` between layers)
- `@portal/models` for shared Zod contracts
- Do not apply to generated shadcn files under `components/ui/` unless editing intentionally
