---
description: AI layer first; feature docs = pilot; app code only when user asks
alwaysApply: true
---

# Portal AI platform

- **Default:** `.cursor/rules`, `.cursor/skills`, `.cursor/extracts`, `docs/operational/`, `scripts/spec|docs/`.
- **App** (`pages/`, `components/`, …): base done — edit only on explicit feature request.
- **`docs/features/**`:** guinea pig — no backward-compat for old spec paths; delete blockers.
- **Legacy repos:** read-once into trace; portal yaml is disposable.
- **App work:** apply `portal-invariants.mdc` + glob rules. Detail: `/portal-ai-platform` skill.
