---
description: Nuxt 4 layers, UI tiers, E2E testid, import alias — pointers to glob rules
alwaysApply: true
---

# Portal Invariants

Nuxt 4 auth-first. Detail: `docs/operational/ARCHITECTURE.md`, `E2E-TESTIDS.md`, `portal-base` skill.

## Layers (bắt buộc)

`pages/components` → `composables` → `services` + `stores` → `models` + `validations` → `$apiFetch`

- No `$apiFetch` in page/component · no reverse import from `models/`
- Form: `useApiForm` + `validations/` + `models/` · contract: `portal-contract-naming.mdc`
- Add feature: `models` → `services` → `stores?` → `composables` → `validations` → `pages` + `testId`

## UI · size · E2E · imports

- Tiers: `ui/` → `molecules/Mo*` → `organisms/Data*|OrGlobal*` · dashboard = `layouts/dashboard.vue` shadcn only
- Registry: `shared/portal-design.registry.json` · `#shell: DataListPage` · `portal-design-vocabulary.mdc` (glob)
- ~200 lines/file · ~20 lines/fn → `portal-code-size.mdc` (glob)
- Playwright + `data-testid` before spec → `portal-base-ui-testid.mdc`, `portal-base-e2e.mdc` (glob)
- Imports: `@/` `~/` `#` — no `../` → `portal-import-alias.mdc` (glob)
