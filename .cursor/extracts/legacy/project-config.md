# Project maps (code lane — thin)

Full archaeology / legacy-spec extracts live on **base-docs** (`.cursor/extracts/legacy/`).

For `/api` `/wire` `/prototype`: resolve `platform-repos.json` (and optional `legacy-repos.json` for brownfield) progressively — one root at a time. Hub: `base-docs/platform/toolchain/PROJECT-MAPS.md`.
