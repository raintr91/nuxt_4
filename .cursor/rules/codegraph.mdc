---
description: CodeGraph MCP — structural code explore (opt-in; not every chat)
alwaysApply: false
---
<!-- CODEGRAPH_START -->
## CodeGraph

Khi hỏi **cấu trúc** source (call graph, định nghĩa, blast radius): ưu tiên `codegraph_explore` trước Grep/Read.

- Cross-repo: một `projectPath` qua `legacy/project-config.md` — không dump full maps.
- Trust AST index; chỉ Grep khi miss.
- **Không** thay spec/grill/legacy-dynamics trên docs hub — CodeGraph = evidence source, không SSOT domain.

Chưa có index: hỏi chạy `codegraph init` (WSL: xem `base-docs/platform/toolchain/CODEGRAPH.md`).
<!-- CODEGRAPH_END -->
