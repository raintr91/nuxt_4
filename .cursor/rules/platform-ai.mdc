---
description: Code-lane harness bias — gen/MCP first; app only when asked
alwaysApply: true
---

# Platform AI

- **Harness SSOT:** `.cursor/` trên **repo code đang mở**.
- **Gen / analyze / tags:** Artifactgraph MCP + `pnpm portal:gen --id` / `unit-gen` / `testcase:gen` — AI = gap (`#needs-*`, Mo*).
- **Skills code:** `/prototype` · `/grill-prototype` · `/platform-base` · `/platform-mark` · `/wire` · `/test` · `/unit` · `/model` (+ `/api` trên BE/fullstack).
- **Không** author handbook/spec/plans trên code repo.
- Bảo trì harness: `/platform-ai`.
