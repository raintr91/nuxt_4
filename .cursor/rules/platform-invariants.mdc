---
description: Nuxt 4 layers, UI tiers, testId — FE code lane only
globs: pages/**/*,components/**/*,composables/**/*,services/**/*,stores/**/*,models/**/*,validations/**/*,layouts/**/*
alwaysApply: false
---

# Platform Invariants (FE)

Nuxt 4 auth-first. Hub: `base-docs/platform/toolchain/ARCHITECTURE.md` · skill `/platform-base`.

## Layers

`pages/components` → `composables` → `services` + `stores` → `models` + `validations` → `$apiFetch`

- No `$apiFetch` in page/component · no reverse import from `models/`
- Form: `useApiForm` + `validations/` + `models/` · `platform-contract-naming.mdc`
- Feature order: models → service → store? → composable → validations? → page + testId
- **Gen first** (`portal:gen`); AI chỉ gap chưa có template/registry.

## UI · size · E2E · imports

- Tiers: `ui/` → `molecules/Mo*` → `organisms/Data*|OrGlobal*` · `layouts/dashboard.vue`
- Registry: `registries/design.registry.json` · `platform-design-vocabulary.mdc`
- ~200 lines/file · `platform-code-size.mdc`
- testId / E2E: `platform-base-ui-testid.mdc`, `platform-base-e2e.mdc` (glob test)
- Imports: `@/` `~/` `#` — `platform-import-alias.mdc`
