---
description: /test — Playwright E2E refinement
globs: tests/e2e/**/*
alwaysApply: false
---

# /test — E2E

Skill: `.cursor/skills/test/SKILL.md` · Grill: `.cursor/skills/grill-test/SKILL.md`

- `getByTestId()` only · Page Object · testcase YAML = E2E only (no legacy inputs)
- Vertical slice; scoped `pnpm test:e2e` before done
- Extracts: resolve `extractBundle: test` → `.cursor/extracts/extract-registry.json`
