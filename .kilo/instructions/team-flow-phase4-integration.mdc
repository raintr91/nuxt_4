---
description: /wire API integration — replace mocks with real services
globs: "{services,composables,models}/**/*.ts"
alwaysApply: false
---

# /wire — Integration

Skill: `.cursor/skills/wire/SKILL.md`

- 4 tầng: models → services → composables → pages (`platform-base`)
- Source requirement: `base-docs Code / `--id``
- Xóa import mock khỏi production path
- Verify: typecheck + `npx playwright test` (scope feature)
