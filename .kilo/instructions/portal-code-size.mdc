---
description: Portal — ~200 lines/file, ~20 lines/function; split when exceeded
globs: "src/**/*.{tsx,ts},tests/**/*.{ts,tsx}"
alwaysApply: false
---

# Code Size

Soft limits — split in the same PR when exceeded.

| Vượt ~200 dòng/file | Tách |
|---------------------|------|
| Page `page.tsx` | Hook + sub-component |
| Hook | `useXxxForm`, `useXxxList`, … |
| Service / store | File theo aggregate; action phức tạp → service |
| E2E spec | Page Object + helper |

| Vượt ~20 dòng/function | Tách |
|------------------------|------|
| Submit handler | `buildPayload`, `validateBeforeSubmit`, `handleApiError` |
| Map/transform | Pure helpers / `useMemo` |

No padding files with blank lines. Component split: `portal-component-split.mdc`.
