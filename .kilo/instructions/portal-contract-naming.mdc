---
description: Same keys FE/API/BE/DB; nested relationships preserved
alwaysApply: true
---

# Contract Naming

Same keys across FE schema, service params, API, BE DTO, DB, pivot payloads.

Cross-repo only when root `team-projects.json` maps `contract.frontend` + `contract.backend` (group e.g. `factory-ai-stack`); else workspace-only + say FE↔BE unavailable. Resolve order: `docs/operational/PROJECT-MAPS.md`.

- No rename layers (`content` not `content_blog`)
- No flatten `auth: { id, name }` → `auth_id`, `auth_name`
- M-N under relationship keys; third-party mapping only at adapter + documented reason

```ts
// Good: { content, auth: { id, name } }
// Bad:  { content_blog, auth_id, auth_name }
```
