# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Some local runs created by CLI flags (Nuxt có thể coi tên này là rootDir nếu truyền sai cờ)
--host/
--port/

# Node dependencies
node_modules
.pnpm-store

# Accidental Nuxt CLI output (wrong flags)
--host/
--port/

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Storybook
storybook-static

# Generated stories (keep hand-written stories in /stories)
stories/auto/

# Cypress artifacts
playwright-report/
test-results/
blob-report/
playwright/.cache/

# Nuxt E2E build dir (tách khỏi .nuxt dev)
.nuxt-e2e

# Local env files
.env
.env.*
!.env.example
!docker/.env.example
node_modules
docker/.env

# Local project maps (repo root SSOT — see docs/operational/PROJECT-MAPS.md)
platform-repos.local.json
legacy-repos.local.json
.cursor/legacy-repos.local.json
.cursor/platform-repos.local.json

# Legacy vendor install paths (removed — use .cursor/extracts/ instead)
.cursor/skills/vendor/
.cursor/rules-vendor/
.cursor/skills-vendor/

# Optional Kilo mirror (SSOT: .cursor/skills|rules|extracts)
.kilo/skills/
.kilo/instructions/
.kilo/extracts/
.kilo/command/

# CodeGraph local index (do not commit)
.codegraph/
.codegraph-*/


# Generated per machine by package init
.cursor/mcp.json
.mcp.json
