/**
 * Prisma model pointer — define model in schema.prisma; this stub documents mapping.
 * Replace with PrismaService queries in handlers.
 */
export type {{entityPascal}}Record = {
  id: number
  name: string | null
  created_at: Date | null
  updated_at: Date | null
}
