# testcase-gen partials

Semantic + axe assertion **code** is generated in `lib/semantic-plan.mjs` from `registries/e2e-test.registry.json`.

Handlebars partials for functional steps remain in `spec.ts.hbs`. Future flow partials (delete, confirm dialog) will land here as `partials/flow-*.hbs`.
